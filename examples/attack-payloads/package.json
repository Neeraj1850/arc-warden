{
  "name": "@agent-warden/attack-payloads",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "local": "tsx src/run-payloads.ts --local",
    "api": "tsx src/run-payloads.ts --api",
    "dev": "tsx src/run-payloads.ts --local",
    "build": "tsc -p tsconfig.json",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "tsx src/run-payloads.ts --local",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@agent-warden/core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
