{
  "name": "@agent-warden/mock-agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx src/index.ts",
    "safe": "tsx src/index.ts safe-transfer",
    "malicious": "tsx src/index.ts malicious-approval",
    "arc:safe": "tsx src/index.ts arc-safe-transfer",
    "arc:malicious": "tsx src/index.ts arc-malicious-approval",
    "build": "tsc -p tsconfig.json",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "tsx src/index.ts --dry-run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  },
  "dependencies": {
    "@agent-warden/x402": "workspace:*"
  }
}
