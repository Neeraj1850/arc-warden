# Prompt Injection Demo

Demonstrates the target scenario where an external tool or agent response tries to change transaction details after the original user intent.

Expected result: AgentWarden blocks mismatched recipient, spender, chain, token, or amount fields regardless of the text explanation supplied by another agent.
