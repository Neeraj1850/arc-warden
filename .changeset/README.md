# Changesets

Use changesets to document package-level changes before releases.

```bash
pnpm changeset
```

AgentWarden is still pre-release, so this folder starts as release-process metadata. Add a new changeset for each meaningful package behavior change once public versioning begins.
